[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ft_package"
version = "0.0.1"
description = "A demo package"
authors = [
  {email = "oezzaou@student.1337.ma"}
]

readme = "README.md"
license = { text = "MIT" }
